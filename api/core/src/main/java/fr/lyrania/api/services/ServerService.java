package fr.lyrania.api.services;

public class ServerService {

    /*public void setStatus(ServerStatus serverStatus) {
        SocketsService.INSTANCE.sendToServer(Channels.SERVERSTATE, out -> {
            out.writeInt(Bukkit.getPort());
            out.writeByte((byte) serverStatus.ordinal());
        });
    }*/
}
